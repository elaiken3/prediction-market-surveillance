# Copy to .streamlit/secrets.toml for LOCAL runs, or paste into the
# Streamlit Community Cloud app's Secrets box. NEVER commit the real file.
motherduck_token = "paste-a-READ-ONLY-motherduck-token-here"
md_database = "market_surveillance"
